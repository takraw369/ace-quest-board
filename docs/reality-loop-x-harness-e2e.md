# X Reality Loop E2E acceptance

PASS requires one real approved content asset to complete:

`CONTENT_OS -> publish_queue -> X -> provider_publish_id -> metric snapshot -> feedback/insight routing`

For manual publish mode, PASS also requires automatic reconciliation without MASA pasting the post URL.

External account action remains blocked until credentials are configured. No test post is created automatically by this branch.